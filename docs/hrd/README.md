# HRD — human-readable documents and presentation content

Operator-saved screenshots, provider logs and other explanatory artifacts.
They are not normative.

## V1 leftovers (not the V2 proof)

These two files are old **V1** frozen-workbench artifacts. They use the
frozen observe contract (`evidence-observe-artifact-input/3`,
`segmentCoverage`, `sourceSegmentId`). They are not the ACME-0154 V2 run
and are not part of
[the POC #1 reusable-execution proof](../acceptance/poc-1-reusable-execution-proof.md).

| File | What it is |
| --- | --- |
| `openAI_log.md` | One V1 extract of a `source-A` exhibit window. |
| `openAI_runtime-jobs.png` | OpenAI Responses dump of V1 validation-repair loops. |
| `desktop.ini` | Windows Explorer sidecar committed with them. Not evidence. |
