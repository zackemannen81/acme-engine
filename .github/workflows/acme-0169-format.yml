name: ACME-0169 canonical formatter

on:
  push:
    branches:
      - felix/acme-0169-runnable-runtime-composition

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: felix/acme-0169-runnable-runtime-composition
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm prettier --write apps/cli/src/acme-runtime-service.ts tests/integration/acme-runtime-service.test.ts
      - run: rm .github/workflows/acme-0169-format.yml
      - name: Commit canonical formatting and self-delete
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/cli/src/acme-runtime-service.ts tests/integration/acme-runtime-service.test.ts .github/workflows/acme-0169-format.yml
          git commit -m "ACME-0169: apply canonical formatting"
          git push origin HEAD:felix/acme-0169-runnable-runtime-composition
