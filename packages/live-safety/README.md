# @acme/live-safety

Pure provider-neutral primitives shared by live application surfaces:
recursive credential-field refusal, explicit opt-in parsing, environment-only
credential resolution and nested run/confirmation/deployment budget checks.
The package performs no I/O and owns no product authorization.
